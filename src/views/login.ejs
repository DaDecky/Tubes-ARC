<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login</title>
  <link rel="stylesheet" href="/styles/login.css">
</head>

<body>
  <div class="container">
    <% if (messages.error) { %> <%= messages.error %> <% } %> <% if
      (messages.signup_msg) { %> <%= messages.signup_msg %> <% } %>
    <form action="/api/auth/login" method="POST">
      <div class="row">
        <input id="1" type="text" name="username" placeholder="username" />
      </div>
      <div class="row">
        <input id="2" type="password" name="password" placeholder="password" />
      </div>
      <div class="row">
        <button type="submit">Login</button>
      </div>
    </form>
    <div class="row">
      <p>Belum punya akun? <a href="/signup">Sign Up</a></p>
    </div>
  </div>
</body>

</html>